<html lang='en'>

<head>
  <meta charset='UTF-8' />
  <meta name='viewport' content='width=device-width, initial-scale=1.0' />
  <title>Project 2: Barn Find</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400&display=swap" />
  <link href="/css/output.css" rel="stylesheet">

</head>

<body class="bg-white h-screen">
  <div class="">
    <header class="pt-3 pb-3 bg-black">
      <nav class="flex justify-between item-center w-[95%] mx-auto">
        <div>
          <img class="pt-2 w-48" src="/img/Untitled-1-01.png">
        </div>
        <div>
          <ul class="flex items-center gap-[55px] text-white">
            <li>
              <a href="">Home</a>
            </li>
            <li>
              <a href="">Carpedia</a>
            </li>
            <li>
              <a href="">Collectors</a>
            </li>
            <li>
              <a href="">Listings</a>
            </li>
            <li>
              <a href="/login-page.js">Log In</a>
            </li>
            <div>
              <button class="bg-[#E0D5C0] text-[#1C1C1C] px-5 py-1 rounded-md hover:bg-[#CFAE70]">Sign Up</button>
            </div>
          </ul>
        </div>
        <!-- Conditionally render login or logout links -->
        {{!-- {{#if logged_in}}
        <a href='/dashboard'>Dashboard</a>

        <a href='/profile'>Profile</a>

        <button class='' id='logout'>logout</button>
        {{else}}
        <a href='/login'>Login or Sign-up</a>
        {{/if}} --}}
      </nav>
    </header>

    <main class=''>
      <!-- Render the sub layout -->
      {{{body}}}
    </main>
    
    <footer>

    </footer>
  </div>

  {{#if logged_in}}
  <script src='/js/logout.js'></script>
  {{/if}}
</body>

</html>